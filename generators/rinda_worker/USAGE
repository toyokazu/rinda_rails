Description:
    Stubs out a new worker. Pass the worker name, under_scored.

    This generates a worker class in app/workers and a worker test
    suite in test/unit/workers.

Example:
    `./script/generate rinda_worker data_analyzer`

    Data Analyzer worker.
        Worker:     app/workers/data_analyzer_worker.rb
        Test:       test/unit/workers/data_analyzer_worker_test.rb

